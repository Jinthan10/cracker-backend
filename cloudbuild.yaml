#steps:
#  - name: 'gcr.io/cloud-builders/docker'
#    args: ['build', '-t', 'gcr.io/$PROJECT_ID/backend:latest', '.']

#images:
#  - 'gcr.io/$PROJECT_ID/backend:latest'
#options:
#  logging: CLOUD_LOGGING_ONLY 
steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/backend:latest', '.']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/backend:latest']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - deploy
      - backend
      - --image=gcr.io/$PROJECT_ID/backend:latest
      - --platform=managed
      - --region=us-central1
      - --allow-unauthenticated
      - --add-cloudsql-instances=your-project-id:your-region:your-sql-instance
      - --set-env-vars=PORT=8080,DB_USER=youruser,DB_PASS=yourpass,DB_NAME=yourdb

images:
  - 'gcr.io/$PROJECT_ID/backend:latest'

options:
  logging: CLOUD_LOGGING_ONLY



